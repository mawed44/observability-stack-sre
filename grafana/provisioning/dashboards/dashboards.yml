# Ce provider demande à Grafana de charger les dashboards du dossier monté.
apiVersion: 1

providers:
  - name: Observabilité
    folder: Observabilité
    folderUid: observabilite
    type: file
    options:
      path: /var/lib/grafana/dashboards
